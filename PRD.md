# God View 产品需求文档（PRD）

> 文档状态：MVP 产品范围已确认 / 待技术验证
> 产品形态：VS Code 扩展
> 目标版本：MVP
> 更新时间：2026-08-07

## 1. 产品概述

### 1.1 背景

在 Agent 辅助开发逐渐成为常态后，开发者能够更快地产生和修改代码，但也更容易失去对项目结构、模块职责、依赖关系和变更原因的整体理解。代码可以运行，并不代表开发者清楚它为什么这样组织、某次 Agent 操作影响了哪里，以及潜在风险位于何处。

God View 是一款面向 Agent 开发时代的 IDE 可视化扩展。它不以“插件独立扫描并理解所有代码”为主要路径，而是让 Agent 在创建和修改项目时，同步上报模块、职责、依赖、变更意图和影响范围。插件将这些结构化事件持续渲染为项目地图，并使用文件系统、Git 和轻量静态分析校验地图与真实代码是否一致。

God View 让开发者能够：

- 从仓库、目录、模块、文件逐层查看项目结构；
- 理解模块之间的依赖、调用与数据流关系；
- 查看一次代码变更影响了哪些模块；
- 在具备数据来源时，回放 Agent 的操作过程和代码改动；
- 直接在地图上标注问题、提出修改建议或要求 Agent 解释；
- 从可视化节点快速返回代码，完成理解、定位和审查。

产品首版基于 VS Code 扩展实现，优先采用二维图。三维视图仅作为后续探索方向，不作为 MVP 的必要能力。

### 1.2 一句话定义

**让 Agent 在写代码的同时维护项目地图，让开发者实时看见项目如何被构建。**

### 1.3 产品价值

| 用户问题                              | God View 提供的价值                  |
| ------------------------------------- | ------------------------------------ |
| 不知道项目由哪些部分组成              | Agent 持续声明并更新分层项目地图     |
| 不清楚模块之间如何关联                | 展示 Agent 声明的语义关系及代码证据  |
| Agent 改了很多文件，难以审查          | 聚合变更并展示影响范围               |
| 知道文件变了，但不知道为什么          | 关联任务、操作事件、Diff 和时间线    |
| 发现问题后还要重新向 Agent 描述上下文 | 在节点或关系上直接发起解释与修改任务 |
| 图很直观，但无法继续工作              | 节点可跳转源码、Diff 和引用位置      |

## 2. 可行性结论

### 2.1 总体结论

该产品具备较高实现可行性。采用“Agent 驱动、扫描校验”后，最大的工作从跨语言代码理解，转移为 Agent 接入协议、状态一致性和可视化：

- **Agent 驱动的项目结构：高可行性。** Agent 在掌握任务上下文时直接声明模块职责、依赖和本次变更，比插件事后仅凭 AST 推测业务语义更准确，也天然支持动态语言和跨语言项目。
- **Agent 接入：中高可行性。** 可通过 MCP 工具、CLI、SDK 或结构化文件接入；真正风险是不同 Agent 的工具调用能力、协议遵循程度和事件完整性不一致。
- **轻量代码校验：高可行性。** VS Code、文件系统和 Git 足以确认文件是否存在、Diff 是否匹配、路径是否变化。静态分析只用于验证显式依赖和发现漂移，不承担完整理解项目的任务。
- **已有项目首次建图：中等可行性。** Agent 可先阅读项目并生成初始地图，插件再校验；准确度依赖 Agent 的上下文窗口、探索深度和项目规模，需要允许用户确认与修正。
- **变更影响分析：中高可行性。** 优先展示 Agent 声明的预期影响，再叠加 Git Diff 和已知依赖给出的校验结果；仍不能承诺覆盖所有运行时影响。
- **Agent 工作追踪：高可行性（使用 God View 协议时）。** Agent 主动发送任务、工具事件和结构变化即可形成时间线；未接入协议时，只能降级为文件和 Git 事件。
- **二维可视化：高可行性。** Webview + Canvas/SVG/WebGL 均可实现。对于大型仓库，需要聚合、按需加载和图布局缓存。
- **三维可视化：技术上可行、产品价值待验证。** 3D 会增加导航、性能和可访问性成本，首版投入产出比低。

这一方案显著降低“插件理解所有代码”的难度，但不会完全取消扫描能力。若完全信任 Agent 上报，地图可能因为 Agent 漏报、用户手动改动或执行失败而逐渐偏离真实仓库，因此插件仍需承担低成本的事实校验和漂移提示。

### 2.2 关键产品边界

God View 的结果需要标注数据来源与可信度：

| 结果类型   | 示例                               | 可信度表达               |
| ---------- | ---------------------------------- | ------------------------ |
| 代码事实   | 文件路径、显式 import、Git Diff    | 标注“代码已验证”         |
| Agent 声明 | 模块职责、设计意图、预期依赖与影响 | 标注 Agent、会话和时间   |
| 静态推断   | 可能调用、未声明的影响范围         | 标注“推断”及置信度       |
| 一致性异常 | Agent 声明与文件/Git 结果不一致    | 标注“待同步”或“验证失败” |
| 不可得信息 | Agent 内部思考、未记录的外部操作   | 不推测、不伪造           |

### 2.3 关键术语与状态定义

| 术语              | 严格定义                                                                                             |
| ----------------- | ---------------------------------------------------------------------------------------------------- |
| 项目相关内容      | 工作区中的第一方源代码、项目配置、资源及其语义模块；不包含依赖源码、传递依赖、缓存和构建输出         |
| 全图              | 在同一连续画布中，每个项目相关区域都以实体或分组代表出现；不等于同时展开每个文件和符号               |
| 语义压缩          | 将职责相近的模块、目录或文件组合为可展开分组，并显示数量与摘要；不得静默省略                         |
| 模块              | 具有明确业务/技术职责且能关联真实代码边界的稳定语义单元                                              |
| 架构性外部系统    | 跨进程、网络或持久化边界，且其交互会影响系统结构的数据库、队列或第三方服务；普通代码库依赖不属于此类 |
| 架构基线          | 用户确认的某一 Git 分支地图快照，是后续增量事件的起点                                                |
| God View 托管任务 | 用户从地图发起、由 God View 创建批准令牌和 ChangeSet 的 Agent 任务，受本 PRD 写权限约束              |
| 外部 Agent 会话   | 用户在 God View 之外启动并授权的 Agent 会话；God View 只观察和记录其事件/文件变化，不接管其权限      |
| 代码已验证        | 仅表示路径、文件、显式依赖或 Diff 证据成立，不表示业务职责描述一定正确                               |
| 用户已确认        | 用户接受某个模块划分或说明；与代码验证是两个独立维度                                                 |
| 漂移              | 当前代码事实与地图声明不一致，或存在尚未归类的第一方内容                                             |
| 修改方案撤回      | 取消尚未执行的方案；已经产生的代码改动不由 God View 自动反向修改                                     |

节点和关系必须分别记录 `声明来源`、`代码验证状态`、`用户确认状态` 与 `最后校验时间`，禁止用单个笼统的“可信度”覆盖这些不同概念。

## 3. 产品目标与非目标

### 3.1 MVP 目标

1. **G1 — Agent 持续建图：** 接入 God View 协议的 Agent 能在开发过程中持续创建和更新项目地图。
2. **G2 — 地图回到代码：** 用户可从模块地图逐级定位到文件或具体代码位置，并一键打开源码。
3. **G3 — 来源可信：** 用户可区分 Agent 的设计声明、真实代码证据和系统推断。
4. **G4 — 过程可回放：** 用户可按时间线查看 Agent 如何构建项目，以及每次操作对应的结构变化和 Diff。
5. **G5 — 漂移可发现：** Agent 漏报或用户手动修改代码时，插件能发现地图与仓库的明显漂移。
6. **G6 — 原位协作：** 用户可在地图原位标注问题，并让 Agent 解释或提出修改方案。
7. **G7 — 全项目、低噪声：** 一张地图覆盖全部项目相关内容，并通过分组和语义压缩保持清晰，不把依赖源码和生成噪声带入视图。
8. **G8 — 非功能可信：** 插件对日常编辑的性能影响可控；God View 自身不上传源码，用户选择的 Agent 是否使用云端能力以其数据政策和用户授权为准。

### 3.2 非目标

MVP 不计划：

- 完整替代 IDE 的文件树、全局搜索、Git 客户端或代码审查工具；
- 精确还原所有运行时调用和业务数据流；
- 读取或展示 Agent 未主动提供的内部推理过程；
- 首版支持所有编程语言和所有 Agent；
- 仅凭插件自动扫描，为任意已有仓库生成完全正确的业务架构；
- 未经用户批准自动修改代码或项目架构；
- 在 MVP 中直接执行难以审查和恢复的大规模架构重构；
- 将 3D 视图作为核心交付内容。

## 4. 目标用户

### 4.1 核心用户

- **使用 Agent 开发的独立开发者：** 经常使用 Codex、Claude Code 等 Agent 创建或修改项目，需要快速理解整体结构、审查改动并在地图上继续指挥 Agent。

### 4.2 次要用户

- 需要教学或讲解代码结构的开发者；
- 新加入项目、需要理解陌生仓库的开发者；
- 关注架构边界和改动影响面的技术负责人或代码审查者；
- 同时维护多个项目、需要快速恢复上下文的独立开发者。

## 5. 核心使用场景

### 场景 A：首次理解陌生项目

用户打开已有仓库后执行“Initialize God View”。Agent 读取基础文件清单并分批探索项目，向 God View 声明模块、职责和关系；插件校验相关文件与显式证据。用户确认初始快照后，可点击模块查看职责、关键文件及上下游依赖，再跳转到源码。

### 场景 B：审查 Agent 的一次改动

Agent 开始任务时先在地图声明计划新增或修改的模块。开发过程中节点实时显示“执行中”，完成后插件将声明与 Git Diff 校验。用户在“变更视图”查看 Agent 原计划、实际结构事件、真实文件变化以及未同步项。

### 场景 C：排查模块耦合

用户选择一个模块，开启“聚焦模式”，仅显示其直接依赖、被依赖关系和跨层引用。用户从异常依赖边跳转到具体 import 或调用位置。

### 场景 D：恢复开发上下文

用户重新打开数日前的任务，通过历史会话查看当时 Agent 修改过的模块、相关 Diff 和最终工作区状态，快速恢复上下文。

### 场景 E：在地图上要求解释或修改

用户发现一条不合理的跨模块依赖，直接点击关系边并标注“为什么订单模块直接依赖支付实现？”用户选择“让 Agent 解释”后，Agent 的回答原位显示并关联代码证据。若用户进一步选择“解除这条直接依赖”，系统将标注转成修改任务；Agent 先展示拟修改的节点、文件和影响范围，用户确认后执行，并在地图上回放变化。

## 6. 产品原则

1. **Agent 声明优先：** Agent 在最了解任务和设计意图的时刻提交结构变化，插件不重复猜测其业务语义。
2. **代码事实校验：** 地图是代码的可观测模型，而不是脱离代码的另一套真相；文件和 Git 负责校验 Agent 声明。
3. **先结构，后细节：** 默认展示模块级视图，避免一开始呈现数千个节点。
4. **图必须能回到代码：** 每个有效节点和关系都应尽量提供源码定位或证据。
5. **来源明确：** Agent 声明、代码事实和系统推断使用不同视觉状态，不混为一谈。
6. **本地优先：** God View 默认在本地维护地图和事件，自身不上传源码；用户选择的 Agent 若使用云端能力，必须展示其数据边界并获得授权。
7. **增量事件：** Agent 只提交本次任务产生的结构变化，插件不频繁重建全仓地图。
8. **适配而非绑定：** 协议与传输方式解耦，不绑定单一 Agent 产品。
9. **全项目覆盖、按语义压缩：** 所有第一方项目内容必须在同一张图中有归属；复杂区域以分组节点概括，不以静默丢弃换取简洁。
10. **安全写入：** Agent 修改当前工作区前必须给出方案并获得用户批准，不自动提交代码，也不自动回滚用户已有改动。

## 7. MVP 功能范围

### 7.1 P0：必须交付

#### 7.1.1 God View 项目模型协议

定义独立于具体 Agent 和语言的版本化协议，使 Agent 能够：

- 开始或结束一次开发任务；
- 新增、更新、移动或删除模块、文件、服务和外部系统节点；
- 新增或删除依赖、调用、数据流、包含等关系；
- 为节点声明职责、设计原因、状态和相关源码位置；
- 在写代码前提交“计划变更”，完成后提交“实际变更”；
- 将一次结构变更关联到会话、任务、文件和 Git Diff；
- 失败或中断时结束临时预览并记录 `failed/interrupted`；已经产生的代码改动不自动回滚；
- 查询当前地图，以便后续 Agent 在已有结构上继续工作。

协议中的“模块”定义为：**具有明确业务或技术职责，并关联一个或多个真实代码边界的语义单元。** 模块可以对应应用、服务、package、目录集合或跨目录领域，但不能是没有源码/配置证据的纯文字概念。目录和文件是模块的下级实现节点。模块首次经用户确认后获得稳定 ID；Agent 只能更新其名称、职责和边界，不能仅因措辞变化创建重复模块。

协议要求：

- 每个事件有唯一 ID、协议版本、会话 ID、时间戳和来源；
- 写操作支持幂等，重复提交不会产生重复节点或关系；
- 支持全量快照和增量事件；
- 支持事务式 `change_start` / `change_end`，避免用户看到半成品状态；
- 未知字段可安全忽略，旧版本地图可迁移；
- 传输层可采用 MCP、CLI、SDK 或本地事件文件，核心数据模型保持一致。

Agent 提交的地图事件默认作为“Agent 声明”实时进入工作地图，不要求用户逐条批准；它们不会自动获得“用户已确认”或“代码已验证”状态。已经确认的模块稳定 ID 不允许被 Agent 仅因重命名而删除重建，结构删除和边界大幅变化必须归属于可追溯 ChangeSet。

这里的“让 Agent 渲染”应定义为：**Agent 决定画面中有哪些业务节点、节点代表什么、它们如何关联；插件决定布局、颜色、交互和最终像素。** Agent 输出声明式项目图，而不是直接生成 SVG/HTML。这样既保留 Agent 对项目语义的理解，也能保证界面一致、安全、可搜索并支持增量更新。

Agent 的标准工作契约为：

1. 开始编码前读取当前地图并提交本次计划变更；
2. 每完成一个有意义的模块步骤，提交对应节点与关系事件；
3. 删除、移动或重构代码时同步更新旧节点，不只新增内容；
4. 任务结束时提交实际影响文件和完成状态；
5. 插件返回冲突或漂移时，Agent 修正声明，或明确说明为何保留差异。

#### 7.1.2 Agent 接入与实时渲染

MVP 必须打通 Codex 和 Claude Code 两个正式 Agent Adapter，同时保留一种低门槛兜底方式：

1. **Codex Adapter：** 通过其可用的 MCP 或本地调用能力接入 God View 工具协议；
2. **Claude Code Adapter：** 通过其可用的 MCP 或本地调用能力接入相同协议；
3. **兜底方式：结构化事件文件。** Agent 按约定写入本地 JSON/JSONL，插件监听文件变化后导入，方便无法被插件主动调用的工作流使用。

两个正式 Adapter 必须通过同一套能力契约和端到端测试，均支持已有项目初始化、增量建图、要求解释、提出修改方案和批准后执行；不能只做到“能连接”。

首次接入引导：

- 检测 Codex、Claude Code 是否可用，但不读取其密钥或上传配置；
- 显示每个 Adapter 的主动调用、MCP、取消、流式和权限控制能力，并标明写入是“运行时强制”还是“文件变化监控”；
- 需要修改 Agent/MCP 配置时先展示具体变化并由用户确认，不静默修改全局配置；
- 提供连接自检：读地图、写入测试事件、取消测试和权限测试；
- 自检失败显示具体缺失能力、诊断日志位置和可复制的修复步骤；
- 两个 Agent 可以同时配置，但一次可写 ChangeSet 只能选择其中一个执行。

接入后应支持：

- 在 Agent 修改代码时实时出现、更新或高亮节点；
- 将“计划中”“执行中”“已完成”“失败”“与代码不一致”作为不同状态展示；
- 每个节点显示最后更新它的 Agent、任务和时间；
- Agent 未遵循协议时明确降级，不把普通文件变化冒充 Agent 语义事件。

#### 7.1.3 项目地图

- 分层展示：工作区 → 包/模块 → 目录 → 文件；
- 使用一张连续画布覆盖全部项目相关内容，不通过分页或只选择部分模块来伪装“全图”；
- 默认视图同时展示所有一级业务/技术区域；复杂区域显示为带节点数量和职责摘要的分组节点；
- 随缩放或用户展开逐步呈现包、目录和文件细节，保持同一地图、实体 ID 和空间位置连续；
- 每个纳入范围的第一方源文件必须归属于某个模块/分组；项目配置与资源文件可按用途聚合为配置/资源分组；尚未理解的内容进入显式“未分类/待分析”节点，不得静默遗漏；
- 提供覆盖率说明：已归类、未分类、已排除和校验失败的文件数量及原因；
- 使用节点大小、颜色或徽标表达文件规模、类型和变更状态；
- 支持缩放、平移、拖拽、框选、折叠、展开、返回上级和重置布局；
- 支持按名称搜索和定位节点；
- 支持按语言、目录、关系类型、变更状态过滤；
- 悬停节点显示一句话职责和关键指标，点击后固定详情面板；
- 点击关系边显示关系类型、方向、Agent 说明和代码证据；
- 支持“聚焦此模块”，暂时淡化无关节点，只保留上下游关系；
- 支持面包屑和前进/后退，避免下钻后失去位置感；
- 双击或操作按钮跳转到源文件；
- 大型项目通过语义分组、层级细节（LOD）和视口裁剪保持可交互，但完整项目结构仍属于同一张图；
- 用户手动调整的布局按工作区和 Git 分支保存，Agent 更新语义时不得覆盖用户布局。

#### 7.1.4 交互式讲解与动画

项目地图应提供可选择播放的“讲解模式”，通过镜头移动、节点高亮和关系流动解释项目，而不是仅展示静态拓扑。

MVP 至少支持三类讲解：

1. **30 秒认识项目：** 按“入口 → 核心模块 → 数据存储 → 外部系统”的顺序介绍整体结构；
2. **关键流程播放：** 沿一条业务或数据流依次高亮节点和关系，例如一次请求如何进入、处理并持久化；
3. **本次变更回放：** 按时间展示 Agent 新增、修改、移动或删除了哪些结构，以及实际 Diff 如何产生。

讲解动画规则：

- 一个步骤只突出一个重点，其他内容降低透明度但不完全消失；
- 使用箭头脉冲或粒子移动表达方向，使用节点状态过渡表达创建、修改和删除；
- 每步只显示一句简短说明，建议不超过 24 个中文字符；
- 默认讲解控制在 15–45 秒，可由用户选择 0.5×、1×、1.5×、2×；
- 提供播放、暂停、上一步、下一步、重播、退出按钮和进度指示；
- 用户可随时点击节点暂停讲解并自由探索，返回后从原步骤继续；
- 自动布局、展开和聚焦应使用连续过渡，避免节点瞬间跳位导致认知中断；
- 不使用无意义的循环动画、闪烁或大幅镜头晃动；
- 首次打开不强制自动播放，由用户主动选择“带我了解项目”。

Agent 不直接生成动画代码，而是输出声明式 `GuidedStory`：包含讲解标题、步骤顺序、需要聚焦的节点/边、简短文案和建议镜头。插件统一渲染，从而保证交互一致性和安全性。

#### 7.1.5 地图标注与 Agent 协作

用户可直接在地图节点、关系边、关键流程、空白区域或一次变更上创建标注。标注不是普通便签，而是可以转换为带完整上下文的 Agent 任务。

标注类型：

- **个人说明：** 记录用户自己的理解，不发送给 Agent；
- **要求解释：** 让 Agent 解释某个模块、关系、流程或变更原因；
- **建议修改：** 描述期望结果，由 Agent 分析并提出代码/架构修改方案；
- **风险与疑问：** 标记耦合、命名、性能、安全或设计问题，可稍后交给 Agent 处理。

创建标注时，系统自动附带：

- 被标注的节点、关系、流程或 ChangeSet ID；
- 关联文件及尽可能精确的代码位置；
- 当前地图版本、Git revision 和相关代码证据；
- 必要的一层上下游关系；
- 用户框选区域的节点集合；
- 用户输入的文字及期望操作类型。

用户无需在聊天框重新描述“我指的是哪里”。发送前必须展示将提供给 Agent 的范围，并允许删除无关文件或上下文。

##### 要求解释流程

```text
选择节点/关系 → 添加“要求解释”标注 → 选择 Agent → Agent 分析
→ 回答原位显示 / Agent 请求写入入口
→ 引用证据并继续追问 / 转为修改方案并等待用户批准
```

解释回答要求：

- 首屏优先显示 1–3 句简短结论；
- 可通过“展开证据”查看文件、代码位置和更完整说明；
- Agent 可生成一段临时讲解动画，高亮其回答涉及的模块和路径；
- 无法确认时明确标注“不确定”，不得用推测替代代码事实；
- 解释任务默认没有 God View 写入授权，但不要求 Adapter 必须运行在强制只读沙箱；
- Agent 认为需要修改时调用 `request_write_access`，界面显示“Agent 建议修改”，随后进入修改方案和用户批准流程；
- 在用户批准前 Agent 不应写文件；若底层 Agent 越权写入，God View 通过文件/Git 变化检测并进入 `unexpected_write`，暂停托管任务、展示 Diff，不自动回滚。

##### 建议修改流程

```text
选择目标 → 添加“建议修改”标注 → Agent 分析
→ 返回修改方案与预计影响 → 用户确认执行
→ Agent 创建 ChangeSet 并修改代码 → 地图实时回放
→ Git/文件校验 → 用户查看 Diff → 接受、继续修改或撤回建议
```

修改协作要求：

- “发送建议”和“授权执行修改”是两个明确动作；默认先让 Agent 给出方案，不自动写代码；
- 方案必须说明将新增、修改或删除的地图实体及相关文件；
- 批准内容同时形成预期可写路径/目录作用域；Adapter 支持权限限制时执行强制约束，不支持时进入“监控模式”，在执行前明确提示并通过文件/Git 变化检测越界写入；
- 用户确认后，Agent 必须通过 God View ChangeSet 执行，确保标注、任务、结构事件和 Diff 可追溯；
- Agent 在当前 VS Code 工作区修改文件，不自动创建分支/worktree，不自动暂存或提交；
- 执行期间原标注显示进度，完成后展示结果摘要和验证状态；
- 用户可以追问、补充约束、拒绝或撤回未执行方案、要求调整或标记已解决；
- 已经执行的代码改动由用户通过 VS Code Diff/Git 决定保留或恢复，God View MVP 不自动生成反向修改；
- 一个标注线程只保留必要对话，长回答默认折叠，避免遮挡画布。

标注状态分支：

- 解释：`draft` → `sent` → `answered` → `resolved`；Agent 需要修改时可从 `sent/answered` 进入 `write_requested`；
- 修改：`write_requested` / `draft` → `plan_proposed` → `approved` → `in_progress` → `resolved`；
- WriteAccessRequest：`requested` → `dismissed` / `converted`；`converted` 只表示已经生成 ChangeProposal，不表示用户已批准，也不授予写权限；
- 任一分支均可进入 `rejected`、`cancelled` 或 `needs_clarification`。

MVP 先支持本地个人标注。多人评论、指派、@成员和团队同步放入后续版本。

#### 7.1.6 语义关系与代码证据

- 展示 Agent 声明的模块依赖、调用、数据流和外部系统关系；
- 对可验证关系补充显式 import/export、路径或 Git Diff 证据；
- 区分“依赖”和“被依赖”；
- 点击关系边可查看来源文件及代码位置；
- 支持仅查看一到两层邻接关系；
- 第一方模块之间的主要关系属于全图；普通第三方库只作为节点详情中的依赖信息，不展开其内部文件或传递依赖；
- 数据库、消息队列、支付、身份认证等具有架构意义的外部系统可作为边界节点显示；
- Agent 声明与代码证据不一致时显示漂移状态，不自动静默覆盖。

#### 7.1.7 变更与构建过程视图

- 显示 Agent 声明的变更目的、预期结构和执行状态；
- 读取当前工作区相对 Git 基线的新增、修改、删除状态；
- 在地图上高亮变更文件和所属模块；
- 展示文件数、增删行数和变更类型；
- 关联 VS Code Diff 编辑器；
- 对比“Agent 声明会修改什么”和“Git 实际修改了什么”；
- 同时展示 Agent 声明的影响范围与系统补充推断，并明确区分来源；
- 支持按会话和任务回放结构如何逐步形成。

#### 7.1.8 工作区、Git 分支与并发

- 二次确认、路径作用域和单写租约适用于 God View 托管任务；外部 Agent 会话由其宿主权限控制，God View 不能声称阻止其写入；
- 检测到外部 Agent/未知来源写入时，在地图标为“外部会话/未知来源”，记录 Diff 和漂移，但不显示“God View 已批准”；
- 完整修改闭环要求本地 Git 工作区；无 Git 时可初始化、浏览和要求解释，但禁用执行修改及 Git/Diff 校验；文件存在和显式 import 等本地证据仍可验证；
- 架构基线、增量状态和用户布局按 workspace identity + Git branch 隔离；切换分支时加载对应地图，首次出现的分支可从共同祖先基线派生；
- 架构基线默认保存在 God View 本地存储；用户可显式导出或选择将共享基线提交到仓库；
- 标注、Agent 回答和个人布局默认不进入 Git；
- 开始可写 ChangeSet 时记录 branch、HEAD、暂存区、工作区已有 Diff 和相关文件版本；已有修改标记为“任务前变更”，不得归因给 Agent；
- 若方案涉及已有未提交修改的同一文件，执行前额外提示冲突风险并要求确认；
- 一个工作区同一时间只允许一个可写 ChangeSet；尚未获写入授权的解释任务可以并行；Codex 与 Claude Code 不得并发写同一工作区；
- Agent、用户或脚本在任务中修改相同文件时，ChangeSet 标记为 `conflicted` 并暂停自动继续，等待用户处理；
- 检测到 Agent 写入批准作用域之外的路径时立即暂停任务并标记 `scope_violation`；越界文件进入 Diff 审查，God View 不自动删除或回滚；
- 解释任务在批准前产生文件写入时标记 `unexpected_write`；用户可停止任务、查看 Diff，或基于当前变化重新生成修改方案，但不能事后伪造先前批准；只有 Adapter 提供可靠任务关联证据时才把变化归因给该 Agent，否则来源标为“未知/外部变化”；
- 可写 ChangeSet 期间检测到 Git 分支切换时立即暂停任务，使批准失效，并在新分支加载独立地图；
- 用户在写入前取消，方案进入 `cancelled`；写入后取消或 Agent 中断，只停止后续操作并标记 `interrupted`，保留已产生 Diff 供用户审查，不自动回滚；
- “接受结果”只关闭 ChangeSet 和更新地图验证状态，不执行 `git add`、commit 或 push。

状态机定义：

- ChangeProposal：`proposed` → `approved` / `rejected` / `cancelled` / `stale`；
- ChangeSet：`approved` → `in_progress` → `completed` → `accepted`；验证仍有警告或失败时，用户可在明确确认后选择 `accepted_with_issues`，但相关声明不得标为代码已验证；
- 可写 ChangeSet 执行中可进入 `conflicted`、`scope_violation`、`interrupted` 或 `failed`；尚未创建 ChangeSet 的解释任务若提前写入则进入 `unexpected_write`。解决异常或恢复到可写状态前必须重新校验基线，必要时重新批准。

#### 7.1.9 轻量仓库校验与降级模式

插件扫描不负责完整理解项目，只负责建立和验证最低限度的代码事实：

- 自动识别工作区、文件路径和 Git 状态；
- 遵循 `.gitignore` 和 God View 排除规则；默认排除 `.git`、`node_modules`、`.venv`/`venv`、`vendor`、缓存、覆盖率结果、构建输出、压缩包、二进制和生成产物；
- lockfile、依赖清单和构建配置只用于识别项目边界与依赖，不作为大量普通节点铺到地图上；
- 第三方依赖源码和传递依赖永不进入项目内部图；只有具有架构意义的直接外部系统以单个边界节点表示；
- 若构建产物本身是部署架构的一部分，可显示一个抽象“产物”节点，但不索引或展开产物目录中的文件；
- 第一方源文件即使暂时无法理解，也必须计入“未分类/待分析”，确保项目覆盖可核对；
- 检查地图引用的文件是否存在，发现删除、重命名和未声明的新文件；
- MVP 对 TypeScript/JavaScript 的显式 import 提供校验；其他语言在没有 Validator 时明确标记“未验证”；
- 文件变化后只检查相关节点，不进行频繁全量分析；
- 发现漂移后提示用户让 Agent 同步地图、接受代码状态或手动修正；
- 没有 Agent 接入时，仍可显示目录、文件和 Git Diff，但不生成未经证实的业务模块职责。

#### 7.1.10 已有项目初始化

针对并非由 Agent 从零构建、且还没有 God View 地图的仓库，提供“Agent 辅助首次建图”。插件不内置或强制购买模型，调用用户已经拥有和授权的 Codex 或 Claude Code。

首次建图流程：

1. **选择 Agent：** 检测已安装且受支持的 Agent 适配器；用户选择 Agent、分析范围和是否允许其读取整个工作区。
2. **生成基础事实：** 插件只读收集目录、文件类型、项目清单、README、入口候选、包依赖、Git 和可选的显式 import，形成探索清单。
3. **分层探索：** Agent 先判断仓库形态，再按应用、包或目录分批阅读；大型项目不要求一次将所有代码放入上下文。
4. **生成视觉草图：** Agent 输出结构化 `ProjectOverview`，选择最能解释项目的模块、外部系统和关键流程，而不是生成长篇总结。
5. **约束与校验：** 插件检查节点数量、文字长度、交叉边、文件证据和未覆盖区域；不符合要求时要求 Agent 简化或修正。
6. **用户确认：** 用户可接受、隐藏、合并、重命名节点，或要求 Agent 重新总结某个区域。
7. **保存基线：** 确认后的结果保存为初始架构快照，之后由 Agent 增量维护，并由 Git/文件事实持续校验。

启动前展示所选 Agent、读取范围和数据边界；若 Adapter 能提供上下文、费用或时间估计则一并展示，不能提供时明确标记“由 Agent 侧计费，God View 无法预估”，不得伪造数值。

由于 VS Code 扩展无法天然控制所有第三方 Agent，接入分为两种体验：

- **主动调用：** 适配器能够调用已登录的本地 Agent CLI/API 时，用户可直接在 God View 中选择并启动；
- **引导调用：** 无可调用接口时，God View 生成初始化任务，用户在 Codex、Claude 等 Agent 中执行；Agent 再通过 MCP、CLI 或事件文件回写地图。

插件不得读取或复制用户的 Agent 密钥。若所选 Agent 可能将代码发送到云端，首次执行前必须展示数据边界并获得用户确认。

#### 7.1.11 单图概览生成规则

首次建图的默认产物不是第三方依赖和每个符号混在一起的原始依赖图，而是一张能够在单个视口理解、且覆盖整个第一方项目的“项目概览图”。Agent 负责语义归类，渲染器负责执行以下约束：

- 极小项目可少于 5 个一级模块；一般项目默认展示 5–9 个一级模块；更复杂的项目将相关模块合并为领域/子系统分组，而不是丢弃；
- 外部系统、数据库、消息队列等独立展示，超过 5 个时按类型或领域分组，不能静默省略；
- 默认一级关系边不超过 15 条，只直接展示主依赖和 1–3 条关键业务/数据流程；其余关系聚合到分组边并可展开；
- 一级节点名称建议不超过 8 个中文字符，补充说明不超过 16 个中文字符；
- 普通工具类、测试、配置和资源在概览中以聚合分组表示，不逐文件展开；构建产物不进入地图；
- 使用位置、分组、形状、图标、颜色和箭头承担主要表达，避免用段落解释；
- 默认布局必须在一个视口内显示所有一级区域或其分组代表，不依赖滚动才能确认项目的完整组成；
- 详细文件、次级依赖和较长说明只在点击或下钻时出现；
- 当项目确实无法在一级粒度清楚表达时，在同一画布中使用嵌套分组和缩放层级展开复杂子系统；不能让用户必须切换到另一张互不关联的图才能确认项目是否完整。

概览图优先回答五个问题：

1. 这是什么类型的项目？
2. 用户或外部请求从哪里进入？
3. 哪些是核心业务模块？
4. 数据保存在哪里，依赖哪些外部系统？
5. 最重要的一条业务或数据流如何贯穿这些模块？

建议的默认视觉语法：

| 元素                     | 表达方式             |
| ------------------------ | -------------------- |
| 应用入口/UI/API          | 左侧或顶部入口节点   |
| 核心业务模块             | 中央同色模块组       |
| 数据库/缓存/文件         | 底部存储形状         |
| 第三方服务               | 画布边缘的外部节点   |
| 确定依赖                 | 实线箭头             |
| Agent 概括但未验证的关系 | 虚线箭头             |
| 核心流程                 | 高亮且带序号的路径   |
| 次级细节                 | 默认折叠，点击后显示 |

通用单图骨架示例：

```text
        ┌────────────── 项目边界 ──────────────┐
用户 → [入口/UI/API] → [核心业务模块组] → [数据存储]
                           │   └────────→ [异步任务]
                           └────────────→ [外部服务]
        └──────────────────────────────────────┘
```

实际项目不必套用固定拓扑，但应保持“入口—核心—数据/外部”的阅读方向稳定。

#### 7.1.12 设置与隐私

- 配置排除路径、最大文件大小、校验范围和 Agent 写入权限；
- 配置默认 Agent、调用方式、分析范围和初始化预算提示；
- 分别配置“解释”和“修改”使用的默认 Agent，并配置标注与回答的保存期限；
- 配置动画速度、是否启用粒子效果和“减少动态效果”；
- 配置是否保存本地历史；
- 一键清除地图缓存与活动历史；
- 首次使用时说明收集的数据和存储位置；
- God View 默认不主动上传源码、Diff、命令内容或 Agent 会话；调用已配置 Agent 前单独展示其本地/云端数据边界。
- Adapter 无法确认数据是否离开设备时按“可能发送到云端”处理，并要求用户确认。

默认数据策略：

- 当前架构基线和布局保留到用户主动删除；
- 原始 Agent 事件、回答和 Diff 关联默认保留 30 天，到期后压缩为不含对话正文的结构快照；
- God View 默认只保存 Diff 的 Git revision、文件范围、统计和哈希，不复制保存完整源码 Diff；原始 Git 对象已不存在时明确显示证据不可用；
- 未解决或用户固定的标注保留，解决后 30 天自动清理其对话正文；
- Telemetry 默认关闭；
- God View 不读取或保存 Agent 密钥，也不代收 Agent 使用费用；
- 所有清理操作先展示将删除的范围，并区分本地数据与用户选择提交到仓库的共享基线。

### 7.2 P1：MVP 验证后

- 更多 Agent 适配器与协议 SDK；
- 符号级声明与视图：类、函数、接口及引用关系；
- 架构异常提示：循环依赖、跨层引用、高耦合节点；
- 提交、分支或 Pull Request 级别的变化对比；
- 项目结构快照及历史对比；
- 面向更多语言的代码校验适配器；
- 导出 PNG/SVG/JSON 报告。

### 7.3 P2：探索项

- 运行时调用链或测试覆盖率叠加；
- 团队共享的架构地图和评论；
- 受控的架构重构建议；
- 3D 项目空间；
- 跨仓库依赖视图。

## 8. 信息架构与交互

### 8.1 入口

- Activity Bar：God View 图标；
- Command Palette：`God View: Open Project Map`；
- 空地图引导：`Initialize with Codex`、`Initialize with Claude` 或 `Generate Agent Task`（仅显示可用选项）；
- 编辑器右键菜单：`Reveal in God View`；
- 状态栏：地图同步、校验状态和异常提示。

### 8.2 主界面布局

| 区域       | 内容                                                           |
| ---------- | -------------------------------------------------------------- |
| 顶部工具栏 | 视图切换、搜索、过滤、标注、重新布局、讲解播放、同步与校验状态 |
| 中央画布   | 项目结构节点与关系边                                           |
| 左侧导航   | 层级、视图模式、历史会话                                       |
| 右侧详情   | 节点职责、路径、指标、依赖、标注线程、变更和跳转操作           |
| 底部时间线 | 讲解步骤或 Agent/文件变更事件及对应 Diff                       |

MVP 可将左侧导航放入 VS Code Tree View，将中央画布和右侧详情放入 Webview，以控制实现复杂度。

### 8.3 视图模式

- **概览视图：** 默认首页，用一张低复杂度图解释项目入口、核心模块、存储、外部系统和主流程；
- **讲解视图：** 播放 Agent 生成的项目导览、关键流程和变更故事；
- **审阅视图：** 集中查看标注、待解释问题、Agent 修改方案及处理状态；
- **结构视图：** 强调目录和模块组成；
- **依赖视图：** 强调模块间有向关系；
- **变更视图：** 强调当前 Diff 和可能影响；
- **活动视图：** 强调操作事件、时间顺序和变更归属。

### 8.4 视觉编码

- 节点颜色表达语言或节点类型；
- 节点边框表达 Git 状态；
- 实线表示确定关系，虚线表示推断关系；
- 关系边必须配有方向、类型及可访问的文字说明；
- 动画中的移动方向必须与关系箭头一致，播放结束后恢复静态视觉状态；
- 标注使用小型徽标和状态描边，不在画布上直接铺开长文本；点击后在侧栏显示线程；
- 待解释、待确认方案、执行中和已解决使用形状加文字状态区分；
- 不仅依赖颜色传递信息，以兼顾色觉差异用户。

## 9. 核心数据模型

```text
Workspace
 ├─ BranchMapState
 │   └─ ArchitectureSnapshot
 │       ├─ ProjectOverview
 │       │   ├─ OverviewNode
 │       │   ├─ KeyFlow
 │       │   ├─ GuidedStory
 │       │   └─ VisualHint
 │       ├─ Group / Module / File / Service / ExternalSystem / Unclassified
 │       └─ Dependency / Call / DataFlow / Contains
 ├─ AgentSession
 │   └─ ManagedAgentTask
 │       ├─ WriteAccessRequest / ChangeProposal / ChangeSet
 │       ├─ ArchitectureEvent
 │       ├─ ActivityEvent
 │       └─ RelatedFileChange
 ├─ AnnotationThread
 │   ├─ AnnotationTarget
 │   ├─ UserRequest
 │   ├─ AgentResponse / ChangeProposal
 │   └─ RelatedChangeSet
 └─ ValidationResult
     └─ Evidence / Drift / Conflict
```

关键实体：

| 实体                 | 关键字段                                                                                                            |
| -------------------- | ------------------------------------------------------------------------------------------------------------------- |
| WorkspaceIdentity    | id、rootUris、repositoryIdentity、createdAt                                                                         |
| BranchMapState       | workspaceId、branchKey、baseGitRevision、mapRevision、layoutRevision                                                |
| ArchitectureSnapshot | id、schemaVersion、workspaceId、branchKey、revision、createdAt                                                      |
| ProjectOverview      | projectType、summary、entryNodeIds、primaryNodeIds、keyFlowIds、coverage                                            |
| CoverageReport       | includedSources、includedConfigs、includedAssets、classified、unclassified、excluded、failed、reasons               |
| GuidedStory          | id、type、title、duration、steps、source、generatedAt                                                               |
| StoryStep            | order、focusNodeIds、focusEdgeIds、caption、cameraHint、duration                                                    |
| AnnotationThread     | id、type、status、author、target、messages、createdAt、resolvedAt                                                   |
| AnnotationTarget     | nodeIds、edgeIds、flowIds、changeSetId、codeLocations、mapRevision                                                  |
| ManagedAgentTask     | id、sessionId、annotationId、agentAdapterId、mode、permissionMode、status、startedAt、endedAt                       |
| WriteAccessRequest   | id、taskId、annotationId、reason、expectedScope、status、createdAt、convertedProposalId                             |
| ChangeProposal       | id、writeAccessRequestId、summary、architectureOperations、relatedFiles、risks、validationPlan、status              |
| Node                 | id、type、path、label、responsibility、source、codeValidation、userConfirmation、locations                          |
| Edge                 | id、source、target、type、reason、evidence、codeValidation、userConfirmation                                        |
| VisualHint           | group、importance、preferredPosition、icon、collapsedByDefault                                                      |
| ChangeSet            | id、sessionId、branchKey、intent、status、baseMapRevision、baseGitRevision、preexistingDiffHash、startTime、endTime |
| ArchitectureEvent    | id、operation、targetId、before、after、source、timestamp                                                           |
| ActivityEvent        | id、source、actor、type、timestamp、payload、relatedFiles                                                           |
| ValidationResult     | targetId、status、evidence、checkedAt、validator                                                                    |
| AgentAdapter         | id、capabilities、permissionMode、protocolVersion、privacyPolicy                                                    |

所有节点、关系和事件应保留 `source`，所有校验结果应保留 `evidence`。同一个实体可以同时具有 Agent 声明、代码证据和用户确认，但三个维度独立记录，不可互相冒充。覆盖率以独立的第一方文件清单为分母，不能由 Agent 自报节点数量计算。

## 10. 技术方案建议

### 10.1 总体架构

```text
Agent
 └─ Agent Adapter（Codex / Claude / 其他）
     ├─ God View Tools（MCP / CLI / SDK / Event File）
     └─ Protocol & Event Gateway
         ├─ Graph State Engine
         ├─ Session / ChangeSet Engine
         ├─ Annotation & Agent Task Engine
         ├─ Validation Engine
         │   ├─ Workspace / Git / File Watcher
         │   └─ Optional Language Validator
         ├─ Local Event Log & Snapshot Store
         └─ VS Code Webview UI
```

核心状态流：

```text
Agent 声明计划 → 地图显示“计划中” → Agent 修改代码
→ 插件观察 Git/文件事实 → Agent 提交完成 → 插件校验
→ 一致则显示“已验证”，不一致则显示“待同步”
```

标注协作状态流：

```text
用户标注节点/关系 → 插件生成最小上下文包 → Agent 解释或提出方案
→ 解释原位显示 / 用户确认修改 → 创建 ChangeSet → Agent 执行
→ 地图与 Diff 回放 → 校验 → 标注解决
```

### 10.2 技术选型建议

- **扩展层：** TypeScript + VS Code Extension API；
- **前端：** Webview 内使用 React 或轻量 UI 框架；
- **二维图：** 优先评估 Cytoscape.js、Sigma.js 或基于 Canvas/WebGL 的图库；最终选型需用 1,000/5,000/10,000 节点基准测试决定；
- **Agent 接入：** Codex 与 Claude Code 均通过统一 God View/MCP 能力契约接入，同时提供 JSON/JSONL 文件导入作为兼容路径；
- **Agent 调用：** 适配器仅在官方或稳定接口允许时主动调用本地 Agent；否则生成可执行的初始化任务，由用户在 Agent 侧启动；
- **状态引擎：** 采用 append-only 事件日志生成当前快照，便于回放、幂等处理和故障恢复；
- **校验层：** 文件与 Git 校验为必选；TypeScript Compiler API 仅用于首版显式 import 校验；
- **持久化：** MVP 使用单写者追加 JSONL 事件日志与周期 JSON 快照，按 workspace + Git branch 隔离；通过存储接口保留未来替换空间；
- **Git：** 优先使用 VS Code 内置 Git 扩展可用的公开接口或命令行只读查询，并为无 Git 项目提供降级体验；
- **进程隔离：** 图计算和校验放入 Worker/子进程，避免阻塞 Extension Host。

### 10.3 God View Agent 协议

以下 TypeScript 仅用于说明事件语义；正式协议以配套技术文档规定的 JSON Schema 为唯一真源：

```ts
interface GodViewEvent {
  version: string;
  workspaceId: string;
  branchKey: string;
  sessionId: string;
  eventId: string;
  timestamp: string;
  baseMapRevision?: string;
  baseGitRevision?: string;
  actor?: string;
  type:
    | 'session_start'
    | 'change_start'
    | 'node_upsert'
    | 'node_remove'
    | 'edge_upsert'
    | 'edge_remove'
    | 'annotation_answer'
    | 'write_access_requested'
    | 'change_proposal'
    | 'change_approved'
    | 'change_complete'
    | 'change_accepted'
    | 'unexpected_write'
    | 'scope_violation'
    | 'change_interrupted'
    | 'change_conflicted'
    | 'change_cancelled'
    | 'change_failed'
    | 'session_end';
  summary?: string;
  targetId?: string;
  relatedFiles?: string[];
  payload?: Record<string, unknown>;
}
```

工具返回值必须告诉 Agent 事件是否被接受、当前地图版本以及校验错误，使 Agent 能立即修正，而不是等到会话结束后才发现地图漂移。适配器只能记录 Agent 或宿主明确暴露的数据；命令参数、环境变量和对话内容默认不属于架构协议，除非用户显式开启。

标注协作至少提供以下工具：

- `get_annotation_context(annotationId)`：读取用户授权的目标、邻接关系、源码位置和问题；
- `answer_annotation(annotationId, answer, evidence)`：提交解释及证据，不授予代码写权限；
- `request_write_access(annotationId, reason, expectedScope)`：Agent 在解释中发现需要改动时请求写入入口，只创建权限升级请求，不授予权限；
- `propose_change(annotationId, proposal)`：提交预计结构操作、文件范围、风险和验证方式；
- `start_approved_change(proposalId)`：仅在方案已获用户批准时创建 ChangeSet；
- `resolve_annotation(annotationId, result)`：在解释完成或修改验证通过后提交处理结果。

上下文包默认遵循最小化原则，只包含标注目标、一层邻接关系和相关代码位置。执行前若地图 revision 或 Git 基线已变化，方案必须标记为过期并重新确认，避免 Agent 按旧上下文修改代码。

### 10.4 老项目首次建图管线

首次建图使用受约束的两阶段输出，避免 Agent 一边阅读一边随意改图：

1. **RepositoryInventory：** 插件生成客观清单，包含项目配置、语言分布、候选入口、包、目录规模和 Git 信息。
2. **ProjectOverviewDraft：** Agent 根据清单和分批阅读结果提交视觉摘要草稿，包含主要节点、聚合关系、关键流程、源码证据及未分析区域；插件据此生成最终 ProjectOverview 与 CoverageReport。

`ProjectOverviewDraft` 至少应包含：

```ts
interface ProjectOverviewDraft {
  projectType: string;
  nodes: Array<{
    id: string;
    type: 'entry' | 'group' | 'module' | 'storage' | 'external' | 'unclassified';
    label: string;
    shortPurpose?: string;
    fileRefs: string[];
    evidenceRefs: string[];
    uncertainties?: string[];
  }>;
  edges: Array<{
    id: string;
    from: string;
    to: string;
    type: 'calls' | 'depends_on' | 'reads' | 'writes' | 'publishes';
    evidence?: string[];
  }>;
  keyFlows: Array<{ label: string; nodeIds: string[] }>;
  guidedStories: Array<{
    title: string;
    type: 'project_intro' | 'key_flow';
    steps: Array<{
      focusNodeIds: string[];
      focusEdgeIds?: string[];
      caption: string;
    }>;
  }>;
  unreviewedAreas: string[];
}
```

Agent 只能提交证据引用和不确定项，不能自行写入 `CoverageReport`、`codeValidation` 或 `userConfirmation`；这些状态分别由 Inventory、Validator 和用户操作产生。Schema 负责限制复杂度；布局引擎根据节点类型和关键流程计算位置。每个讲解建议 3–8 步，步骤只能引用地图中已有的节点和关系。Agent 可给出分组、重要性和镜头建议，但不直接输出可执行 HTML、SVG、动画脚本或任意样式代码。

### 10.5 大型仓库策略

- 首次初始化按目录或子项目让 Agent 分批提交快照；
- 完成全仓第一方文件清单后再声明初始化完成，未分析区域必须作为可见分组存在；
- 按职责和代码边界聚类，默认显示全部一级分组及其数量/摘要；
- 采用层级细节（LOD）：远景渲染分组，中景渲染模块，近景按需渲染目录/文件；所有层级属于同一张图；
- 使用视口裁剪减少绘制成本，但搜索、覆盖率和导航仍包含画布外实体；
- 边按层级聚合，例如 30 条文件引用可在远景表示为一条带计数的模块关系；
- 不通过固定节点上限静默丢弃项目内容；性能不足时优先降低动画、聚合关系和折叠细节，并明确提示当前粒度；
- 事件只更新相关节点，定期压缩为快照；
- 文件变化时仅校验该文件关联的节点和关系；
- 提供“低资源模式”，暂停后台深度分析。

## 11. 非功能需求

### 11.1 性能目标

以排除依赖目录后的有效源文件计：

| 指标                              | MVP 目标                       |
| --------------------------------- | ------------------------------ |
| 接收 Agent 事件到地图可见         | P95 ≤ 300 ms                   |
| 5,000 节点快照首次加载            | P95 ≤ 3 秒                     |
| 10,000 个语义实体的全图概览可交互 | P95 ≤ 5 秒                     |
| 文件保存后的增量校验              | P95 ≤ 1 秒                     |
| 已缓存工作区再次打开              | P95 ≤ 3 秒可交互               |
| 常规项目动画帧率                  | 目标 60 FPS，最低不低于 30 FPS |
| 点击、暂停和切换步骤的视觉反馈    | P95 ≤ 100 ms                   |
| Extension Host 峰值阻塞           | 单次同步任务 ≤ 100 ms          |
| 第一方纳入文件的地图归属覆盖率    | 100%（允许归入“未分类”）       |
| 被排除第三方依赖的内部文件节点    | 0                              |

以上为产品目标，需通过原型基准测试校准，不能在验证前视为已承诺指标。

### 11.2 可靠性

- 单个事件或文件校验失败不应导致全局地图不可用；
- 单个讲解步骤引用失效节点时应跳过并提示，不中断整张地图；
- 节点移动或重命名后，标注应通过稳定 ID 继续关联；无法重连时标记为“目标已变化”，不得悄悄绑定到其他节点；
- 事件日志损坏时可从最近快照恢复，并隔离错误事件；
- Agent 重试同一事件时不得产生重复节点；
- `change_complete` 前意外中断时，未完成变更保持可识别和可恢复；
- Webview 关闭后不丢失必要状态；
- 无 Git、无 Agent 数据或语言不受支持时提供明确降级说明。

### 11.3 安全与隐私

- 默认仅在本地处理和保存数据；
- 不记录环境变量值、密钥、Token 和被排除文件；
- 命令及 Agent payload 在落盘前执行脱敏；
- 标注发送前展示上下文清单，未经用户确认不得扩展到整个仓库；
- “要求解释”默认没有 God View 写入授权；可使用 Agent 原生只读模式，但不作强制要求；无原生限制时必须显示“监控模式”并检测意外文件变化；
- Agent 请求写入只会打开方案入口，不能跳过用户批准；批准前写入必须显示为 `unexpected_write`；
- 只有处于 `approved` 状态的修改方案才能通过协议创建可写 ChangeSet；
- 历史记录应有保存期限和手动清除入口；
- Webview 配置严格的 Content Security Policy；
- 仅执行插件内置的声明式动画，不执行 Agent 输出的 HTML、SVG 或脚本；
- 仅加载扩展打包资源，避免执行仓库内不可信脚本；
- 远程开发、容器和 SSH 场景下明确数据存储位置。

### 11.4 可访问性

- 核心操作可通过键盘完成；
- 为节点和关系提供文本列表替代视图；
- 讲解模式提供可读的步骤列表和完整文字摘要；
- 遵循系统及 VS Code 的减少动态效果设置，关闭粒子、缩短或取消镜头过渡；
- 播放、暂停、上一步和下一步可通过键盘操作，并向辅助技术播报当前步骤；
- 适配 VS Code 明暗主题与高对比度主题；
- 状态不能只通过颜色区分。

### 11.5 MVP 支持矩阵

| 环境                                              | MVP 支持级别                                                                                                       |
| ------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------ |
| macOS / Windows / Linux 本地 VS Code + Git 工作区 | 完整支持                                                                                                           |
| 本地 VS Code、无 Git                              | 地图浏览、初始化、讲解、解释和本地文件证据；Agent 可提出修改请求，但不能由 God View 执行修改，不提供 Git/Diff 校验 |
| 多根工作区                                        | 支持，但每个 root 有独立 identity、覆盖率和分支状态                                                                |
| VS Code Remote/SSH、WSL、Dev Container            | Beta 验证范围，不作为 MVP 发布阻断项                                                                               |
| 无 Codex、无 Claude Code                          | 可浏览已有地图和代码事实，无法执行 Agent 初始化/解释/修改                                                          |

MVP 对仓库规模不设置“超过即拒绝”的产品上限。性能基准以 5,000/10,000 个语义实体为验证档位；更大项目使用语义分组、LOD 和关系聚合降级，但必须继续展示全项目覆盖率和未分类区域。

## 12. 成功指标

### 12.1 北极星指标

**每周至少完成一次“从地图获得理解或采取开发行动”的独立开发者数。**

一次有效闭环至少满足以下之一：从地图定位并打开源码/Diff；完成讲解后进入相关节点；获得带代码证据的 Agent 解释；从标注创建并批准一个修改方案。单纯打开地图不计入。

### 12.2 辅助指标

- 接入引导后 Agent 成功创建首个节点和关系的比例 ≥ 90%；
- Codex 与 Claude Code 各自完成初始化、解释和批准后修改闭环的成功率；
- 老项目首次建图成功生成合规单图概览的比例；
- 纳入范围的第一方文件归属覆盖率，以及“未分类”占比；
- 第三方依赖内部文件误入地图的数量；
- 概览图一次通过复杂度约束的比例；
- 用户接受初始地图前进行合并、删除或改名的节点数量；
- Agent 任务结束时地图与实际变更文件的一致率；
- 单次任务中需要用户手动修复的漂移数量；
- 地图生成后 5 分钟内发生源码/Diff 跳转的会话比例；
- “带我了解项目”讲解的启动率、完成率和中途转入自由探索的比例；
- 用户观看关键流程后正确复述入口、核心模块和数据去向的比例；
- 地图标注发送给 Agent 的比例，以及解释/修改标注的解决率；
- 从创建解释标注到获得有证据回答的时间；
- 修改方案的接受、调整、拒绝比例及执行成功率；
- 用户是否需要在外部聊天中再次说明标注上下文；
- 周活跃用户的人均地图使用次数；
- 变更视图和活动视图的使用率；
- 事件接收失败率、崩溃率和 P95 渲染延迟；
- 用户完成“理解指定模块”任务的时间，相比纯文件树是否下降；
- 7 日和 28 日留存。

### 12.3 需验证的核心假设

1. 开发者愿意使用独立地图，而不仅依赖文件树和搜索。
2. Agent 能稳定遵循“读取地图—声明变更—编码—提交完成”的工具契约。
3. Agent 主动声明的结构比插件纯扫描结果更接近用户对业务架构的理解。
4. Agent 能把老项目压缩为一张简洁概览图，同时保留足够的结构信息。
5. 5–9 个一级节点和不超过 15 条边是适合大多数项目的默认复杂度。
6. 分步骤高亮关键流程比一次性展示全部关系更容易理解和记忆。
7. 模块级视图比默认展示文件/函数级大图更有帮助。
8. Agent 变更回放能够提升审查效率和信任，而不是增加信息噪声。
9. 在地图上直接标注比复制文件路径到聊天框更快、更少产生上下文误解。
10. Agent 的原位解释和带预览的修改方案能够形成从理解到行动的闭环。
11. 二维视图已经足以承载核心价值，3D 并非早期采用的必要条件。

### 12.4 MVP 产品验证退出条件

在至少 5 名目标独立开发者、3 个不同类型的真实项目上验证：

1. Codex 和 Claude Code 均能完成已有项目初始化、要求解释和批准后修改三条端到端旅程；
2. 所有纳入的第一方文件均有地图归属，第三方依赖内部文件误入为 0；
3. 至少 80% 的用户能在 30 秒内指出主要模块，并在讲解后正确复述一条核心流程；
4. 至少 80% 的解释回答包含可打开的源码证据，且用户不需要去外部聊天重新说明目标；
5. 所有已批准 Agent 修改均能追溯到方案、ChangeSet 和 Diff；批准前写入必须被检测并标记，未被发现的未授权写入为 0；
6. 用户认为地图过密或无法定位主结构的测试任务不超过 20%；
7. 技术质量、性能、安全和可访问性门禁全部通过。

这些阈值用于判断 MVP 是否具备公开验证价值；样本扩大后可基于真实分布调整，但不得删除原始测试结果。

### 12.5 指标采集边界

- Telemetry 默认关闭，因此 MVP 产品验证主要通过受邀用户任务测试、访谈和自愿提交的本地诊断摘要完成；
- 用户主动开启 Telemetry 后只采集事件类型、耗时、计数、结果码和功能开关，不采集仓库名、路径、源码、标注正文、Agent 回答或 Diff；
- 覆盖率、未分类数和性能可在本地计算并展示，只有用户显式同意才上传聚合值；
- 未获得足够样本时指标标记为“数据不足”，不得把缺失数据当成成功。

## 13. 里程碑建议

里程碑以退出条件而不是团队规模或固定周期推进。

### 阶段 0：技术验证

- 定义 God View 事件 Schema 与 5–8 个最小工具；
- Codex 和 Claude Code 均完成 Adapter 能力发现、连接、读图和最小写事件；
- 让两个 Agent 分别完成一次小项目的“读取地图—声明模块—修改代码—完成事件”；
- 让至少一个 Agent 为现有小型项目生成受约束且覆盖完整的单图概览；
- 完成事件接收、快照生成、二维实时渲染和源码跳转；
- 完成一个由 3–5 个声明式步骤组成的关键流程动画原型；
- 完成“标注一条关系—Agent 原位解释—引用源码证据—请求写入入口”的原型；
- 验证事件重试、任务中断和状态恢复；
- 对比 Agent 声明与 Git 实际变更，验证基础漂移检测；
- 用合成数据验证 Webview 在 1,000、5,000、10,000 节点下的表现。

退出条件：Codex 和 Claude Code 均能通过同一协议创建有效地图事件；至少一个 Agent 完成老项目初始化；重放事件得到相同地图；第三方依赖未进入项目图；10,000 个合成语义实体可通过 LOD 在同一画布浏览。

### 阶段 1：MVP Alpha

- 完成结构、依赖、搜索、过滤和源码跳转；
- 完成悬停说明、节点聚焦、上下游筛选和讲解播放控制；
- 完成四类标注、原位回答和标注状态管理；
- 完成“建议修改—方案预览—用户批准—ChangeSet 执行—Diff 验证”闭环；
- 完成计划、执行、完成和失败状态；
- 完成 Git 变更覆盖层与漂移提示；
- 提供 MCP/本地工具接口及事件文件兜底；
- 完成 Agent 使用说明或自动注入的项目指令模板；
- 完成空地图引导、Agent 选择、初始化进度与用户确认界面；
- Codex 和 Claude Code 均完成初始化、解释、方案和批准后修改的端到端契约测试；
- 完成分支隔离、任务前已有修改识别、单写 ChangeSet 和中断恢复；
- 邀请 5–10 名目标用户内部试用。

退出条件：两个 Agent 的四条核心旅程均可用；单个真实项目能够全覆盖建图、讲解、标注和修改；未被发现的未授权写入、第三方依赖噪声和静默遗漏均为 0。

### 阶段 2：MVP Beta

- 增加 TypeScript/JavaScript 显式依赖校验；
- 完成默认 30 天历史清理、共享基线导出和分支状态恢复；
- 完成 macOS、Windows、Linux 本地 Git 工作区验证；
- 完成隐私设置、故障注入、异常恢复和大图性能优化；
- 按第 12.4 节使用真实任务验证理解、解释和修改效率。

退出条件：第 12.4 节产品验证与配套代码质量规范的发布门禁全部通过。

### 阶段 3：公开版本

- 通过 VS Code Marketplace 发布，并提供可安装 VSIX 作为回退分发方式；
- 发布隐私说明、支持矩阵、已知限制、CHANGELOG、数据迁移和回滚说明；
- 根据 Beta 数据决定优先扩展 Agent、语言校验器或架构分析；
- 仅在二维交互价值稳定后评估 3D 原型。

## 14. 风险与应对

| 风险                             | 影响                             | 应对策略                                                                                       |
| -------------------------------- | -------------------------------- | ---------------------------------------------------------------------------------------------- |
| 全项目覆盖导致节点过多、关系混乱 | 用户无法理解，性能下降           | 同一画布使用语义分组、LOD、视口裁剪和关系聚合；覆盖率可核对但细节不同时展开                    |
| 语义压缩把第一方内容误排除       | 地图看似清晰但不完整             | 第一方清单独立核对；所有文件必须归类或进入“未分类”；排除原因可查看                             |
| Agent 忘记或拒绝调用协议工具     | 地图不完整                       | 项目指令模板、工具调用反馈、完成前校验、缺失事件提示                                           |
| Agent 上报的结构不准确           | 用户被错误地图误导               | 与路径和 Git 事实校验；显示来源；支持确认与修正                                                |
| 不同 Agent 对“模块”理解不同      | 同一项目地图不稳定               | 明确 Schema、读取现有地图后再修改、提供模块粒度规范                                            |
| Agent 事件乱序、重试或中断       | 重复节点或半成品状态             | 幂等 ID、版本号、事务式 ChangeSet、事件重放                                                    |
| Agent 无工具接入能力             | 无法获得完整语义                 | JSON/JSONL 兜底；仅展示文件/Git 事实；明确能力等级                                             |
| Codex 与 Claude Code 能力不一致  | 用户体验和协议行为分裂           | 统一能力矩阵和契约测试；能力不足时明确禁用对应操作，不做名称推断                               |
| 插件无法直接调用用户的 Agent     | 首次建图流程割裂                 | 支持适配器主动调用与“生成初始化任务”两种路径                                                   |
| 老项目超过 Agent 上下文能力      | 总结漏掉关键模块                 | 分层探索、按子项目建图、显示未分析区域、允许续跑                                               |
| Agent 生成文字过多或图过密       | 无法一眼看懂                     | Schema 硬限制、复杂度评分、自动要求 Agent 简化                                                 |
| 过度简化隐藏关键架构             | 概览产生误导                     | 显示覆盖率和未分析区域；支持下钻图；用户确认基线                                               |
| 动画过多分散注意力               | 降低理解效率                     | 只为流程、状态和变化使用动画；默认不自动播放；限制时长和步骤                                   |
| 镜头移动或闪烁引发不适           | 可访问性问题                     | 减少动态效果、无闪烁、暂停控制、步骤列表替代体验                                               |
| 大图动画掉帧                     | 破坏理解连续性                   | 聚焦时只渲染相关子图；粒子数量上限；低性能模式自动降级                                         |
| 使用云端 Agent 分析私有代码      | 源码泄露风险                     | 明示数据边界、用户授权、优先复用本地配置、不读取密钥                                           |
| 标注上下文携带过多代码           | 超出用户预期或泄露信息           | 发送前预览、默认一层邻接、允许移除文件、遵循排除规则                                           |
| Agent 解释冗长遮挡地图           | 退化为普通聊天窗口               | 原位仅显示短结论，证据和长回答在侧栏按需展开                                                   |
| “要求解释”意外修改代码           | 破坏用户信任                     | 默认无写授权；提供 `request_write_access`；批准前写入标记 `unexpected_write` 并暂停、展示 Diff |
| 修改方案基于过期地图             | 改错位置或覆盖新改动             | 绑定地图 revision 与 Git 基线；执行前重新校验并确认                                            |
| 分支切换或两个 Agent 同时写入    | 事件归属错误、代码冲突           | 地图按分支隔离；单工作区单写 ChangeSet；切分支立即暂停并使批准失效                             |
| 工作区已有未提交修改             | Agent 变更难以归因或覆盖用户代码 | 记录任务前 Diff；重叠文件二次提示；中断后保留 Diff、不自动回滚                                 |
| 节点变化导致标注失去目标         | 历史讨论无法追溯                 | 稳定实体 ID、重连提示、保留原始位置快照                                                        |
| 文件监听难以准确归因             | 把用户改动误认为 Agent 改动      | 仅在适配器提供身份时标记 Agent；否则标记“未知来源”                                             |
| 用户或脚本绕过 Agent 修改代码    | 地图逐渐漂移                     | 文件/Git 监听、漂移提示、让 Agent 重新同步                                                     |
| 后台校验拖慢 IDE                 | 影响核心开发体验                 | Worker、增量校验、资源上限、暂停开关                                                           |
| 命令或会话包含敏感信息           | 隐私和安全问题                   | 默认最小记录、脱敏、本地存储、清除和关闭入口                                                   |
| 3D 吸引注意但降低效率            | 延迟核心价值交付                 | 先验证二维任务完成率，再决定是否投入                                                           |
| 自动摘要产生幻觉                 | 错误理解架构                     | 摘要与静态事实分层，提供证据链接，首版不作为核心依赖                                           |

## 15. MVP 验收标准

验收至少使用小型、约 500 个源文件的中型、以及可生成 5,000/10,000 个语义实体的大型项目数据集，并分别使用 Codex 和 Claude Code 执行核心旅程。

### 15.1 安装、Agent 与首次建图

1. 安装扩展后可打开 God View，不要求创建 God View 账号，也不要求向插件提供 Agent 密钥。
2. Codex 和 Claude Code 均可被检测/配置，并准确展示是否支持主动调用、MCP、取消、强制权限和监控模式。
3. 两个 Agent 均可完成已有项目初始化；无法主动调用时可生成任务并通过事件文件回写。
4. 初始化显示进度、已覆盖、未分类、已排除、校验失败及其原因，并可取消和继续。
5. 所有纳入的第一方源文件均归属于模块/分组或“未分类”，配置与资源进入对应聚合分组；静默遗漏为 0。
6. `.git`、`node_modules`、虚拟环境、vendor、缓存、构建输出和第三方依赖内部文件节点为 0。
7. 极小项目可以少于 5 个一级模块；一般项目保持 5–9 个；复杂项目使用分组覆盖全部一级区域。
8. 用户可合并、拆分、重命名和确认模块，确认后的稳定 ID 在 Agent 重新措辞后保持不变。

### 15.2 全图、交互与讲解

1. 默认视口能看到所有一级区域或其分组代表，且每个分组显示包含数量和简短职责。
2. 放大、展开和下钻在同一画布连续进行，不以互不关联的分页或子图替代完整项目。
3. 搜索能定位折叠分组内的任一第一方文件，并自动展开到目标。
4. 悬停、详情、上下游聚焦、面包屑、源码/Diff 跳转和用户布局持久化可用。
5. “带我了解项目”能讲解入口、核心模块和数据去向，并支持播放控制和随时转入自由探索。
6. 启用“减少动态效果”后，所有讲解仍可通过静态步骤和文字列表完成。
7. 5,000/10,000 语义实体数据集达到第 11.1 节性能目标；更大项目降级时不丢失覆盖率或搜索能力。

### 15.3 标注、解释与修改

1. 用户可在节点、关系、流程、框选区域和 ChangeSet 上添加四类标注并查看状态。
2. 发送前可预览并删减 Agent 将获得的节点、关系和文件上下文。
3. Codex 与 Claude Code 的“要求解释”首先返回短结论和代码证据；Agent 可请求写入入口，但请求本身不改变文件。
4. 用户批准前解释任务没有 God View 写授权；提前写入会进入 `unexpected_write`、暂停并显示 Diff。
5. “建议修改”只返回节点、文件、风险和验证方案，在用户批准前不获得写入授权。
6. 批准令牌包含预期写入范围；Adapter 支持时强制限制，不支持时明确进入监控模式。
7. 用户批准后 Agent 在当前工作区执行；God View 不创建分支/worktree，不自动暂存、commit 或 push。
8. 修改结果能从原标注追溯到批准方案、ChangeSet、结构事件、Diff 和校验结果。
9. 撤回未执行方案不会修改代码；已执行变更只能由用户通过 Diff/Git 决定保留或恢复。

### 15.4 Git、分支、并发与恢复

1. 开始 ChangeSet 时正确记录 branch、HEAD 和任务前已有 Diff；原有改动不归因给 Agent。
2. 方案与已有修改涉及相同文件时必须提示并再次确认。
3. 同一工作区只能有一个可写 ChangeSet；尚未获写入授权的解释任务可以并行。
4. Codex、Claude Code、用户或脚本并发修改同一文件时，任务进入 `conflicted` 并暂停。
5. Agent 写入批准范围外路径时任务进入 `scope_violation` 并暂停，越界 Diff 可见且不被自动删除。
6. 解释任务批准前写入时进入 `unexpected_write`，不能事后改写为已提前批准。
7. 分支切换会暂停正在写入的任务、使旧批准失效，并加载新分支独立地图与布局。
8. 写入后取消或 Agent 崩溃会进入 `interrupted`，保留已产生 Diff，不自动回滚。
9. 重复、乱序和非法事件得到稳定处理；相同事件日志回放得到相同地图、讲解和标注关联。
10. 重启 VS Code 后未完成任务、分支地图和本地标注状态可恢复。
11. 30 天到期清理后当前地图仍可由压缩快照打开，未解决/固定标注不会被误删。
12. 外部 Agent 会话或未知进程写入不会被伪装成 God View 已批准任务，并能与托管 ChangeSet 区分。

### 15.5 降级、平台与质量

1. 无 Git 时只提供初始化、地图、讲解和解释；Agent 可请求/提出修改，但 God View 执行入口明确禁用。
2. 无 Agent 时可浏览已有地图、目录和 Git 事实，不虚构业务职责、Agent 身份或意图。
3. macOS、Windows 和 Linux 本地 Git 工作区通过核心旅程；Remote/SSH、WSL 和 Dev Container 记录为 Beta 兼容性结果。
4. Agent、语言或环境能力不足时展示具体缺失能力和下一步，不只显示通用错误。
5. 第 12.4 节产品验证退出条件与配套代码质量规范发布门禁全部通过。

### 15.6 需求追踪矩阵

| MVP 目标                | 核心需求                              | 主要验收                     |
| ----------------------- | ------------------------------------- | ---------------------------- |
| G1 Agent 持续维护地图   | 7.1.1、7.1.2                          | 15.1.2–3、15.4.8             |
| G2 从地图回到代码       | 7.1.3、7.1.6                          | 15.2.3–4                     |
| G3 区分声明、事实与推断 | 2.2、2.3、7.1.6                       | 15.1.4–5、15.5.4             |
| G4 回放构建与变更       | 7.1.4、7.1.7                          | 15.2.5–6、15.3.7             |
| G5 发现漂移和并发冲突   | 7.1.8、7.1.9                          | 15.4.1–11                    |
| G6 原位解释和修改       | 7.1.5                                 | 15.3.1–8                     |
| G7 全项目覆盖且保持简洁 | 产品原则第 9 条、7.1.3、7.1.9、7.1.11 | 15.1.4–8、15.2.1–3、15.2.7   |
| G8 性能、隐私和可访问性 | 7.1.12、11                            | 15.2.6–7、15.5、代码质量门禁 |

任何新增 P0 需求必须补充对应目标和可验证验收；无法进入该矩阵的功能不得作为 MVP 必需项。

## 16. 已确认决策与待技术验证

### 16.1 已确认产品决策

- 首要用户：使用 Agent 开发的独立开发者；
- 首批正式 Agent：Codex 与 Claude Code，均须打通完整闭环；
- 模块：关联真实代码边界、用户确认后 ID 稳定的语义职责单元；
- 默认首页：以全项目概览和快速理解为核心；
- 架构基线：默认本地、按 Git 分支隔离，可显式导出/提交共享基线；
- 标注与回答：默认本地，不进入 Git；
- 修改位置：当前 VS Code 工作区；不自动创建分支、提交或推送；
- 修改权限：永远先提交方案并二次确认，MVP 不提供自动执行；
- 解释权限：默认无 God View 写入授权，不强制底层 Agent 使用只读沙箱；Agent 可申请写入入口，但申请、方案、批准和 ChangeSet 必须分离；
- 完整支持环境：macOS、Windows、Linux 本地 Git 工作区；
- 数据策略：基线保留到删除；原始事件/回答默认 30 天后压缩或清理；Telemetry 默认关闭；
- 全图：完整覆盖第一方项目内容，以嵌套分组和 LOD 简化，不渲染第三方依赖内部、缓存和生成噪声；
- 里程碑：按退出条件推进，不以团队规模或固定周期约束。

### 16.2 Stage 0 用数据决定的事项

- Codex 与 Claude Code 在当前可用接口下的主动调用、取消、权限强制/监控和流式能力；
- Cytoscape.js + ELK.js 是否满足全图、LOD、动画与可访问性要求；
- JSONL + JSON 快照在 100K 事件和大型项目下是否需要替换实现；
- 默认分组算法和 5–9 个一级模块经验值是否适用于三类真实项目；
- 首次建图的 Agent 耗时、上下文消耗和费用信息能否从 Adapter 稳定获得；
- Remote/SSH、WSL 和 Dev Container 的实际兼容范围。
- 满足 Webview、Worker、Git 和安全能力所需的最低 VS Code 版本，并在扩展 manifest 明确声明。

技术验证只能在既定产品边界内选择实现方式；若结果要求改变用户权限、数据归属、全图定义或双 Agent 范围，必须重新进行产品决策并记录 ADR。

## 17. 建议的下一步

1. 先写一个最小协议，包含地图更新操作，以及 `get_annotation_context`、`answer_annotation`、`request_write_access`、`propose_change` 和 `start_approved_change` 五个标注协作工具。
2. 为 Codex 和 Claude Code 分别实现最小 Adapter，先通过同一能力发现与契约测试。
3. 让两个 Agent 分别从零实现一个小项目并同步维护地图，验证事件语义一致。
4. 插件只实现事件接收、全项目语义分组、二维渲染、源码跳转和 Git 校验，先跑通完整闭环。
5. 再让两个 Agent 为三个不同类型的已有项目生成单图概览和一段关键流程讲解，记录覆盖率、未分类、第三方噪声、错误关系和人工修正数量。
6. 用 5 秒识别项目类型、30 秒说出主要模块、播放后 2 分钟内复述核心流程三个任务检验“一目了然”。
7. 让用户在一条依赖边上分别完成“要求解释”和“建议修改”，验证是否无需去聊天框补充上下文。
8. 验证任务前已有修改、分支切换、双 Agent 竞争写入、取消和中断恢复。
9. 访谈 5 名 Agent 重度用户，对比“实时项目地图”和“文件树 + Git Diff”的理解与审查效率。
10. 根据结果再决定是否扩展更多 Agent、协议能力和语言校验器，暂缓完整代码扫描器与 3D。

## 18. 配套工程文档

- [工程文档索引](./docs/README.md)
- [技术架构与技术选型](./docs/TECHNICAL_ARCHITECTURE.md)
- [编码与可维护性规范](./docs/CODING_STANDARDS.md)
- [代码质量评估与质量门禁](./docs/CODE_QUALITY_STANDARD.md)
- [架构决策记录模板](./docs/adr/0000-template.md)
